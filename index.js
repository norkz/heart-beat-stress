const readline = require('readline');

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

console.log('Press Any keyboard key to add the value and Arrow_Left to decrease')
setInterval(() => {
  setInterval(() => {
    setInterval(() => {
      setInterval(() => {
        setInterval(() => {
          setInterval(() => {
            setInterval(() => {
              console.log(`${rl.cursor.toString().replace(/^/, 'Heartbeat number: ')}\n${rl.cursor.toString().replace(/^/, 'Heartbeat number: ')}\n${rl.cursor.toString().replace(/^/, 'Heartbeat number: ')}`)
            }, 0);// Interception
          }, 0);// Interception
        }, 0);// Interception
      }, 0);// Interception
    }, 0);// Interception
  }, 0);// Interception
}, 0);// Interception

